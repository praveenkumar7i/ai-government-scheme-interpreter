# API Spec (Starter)

## POST /api/v1/documents/upload
Uploads a government scheme PDF and triggers synchronous ingestion.

## POST /api/v1/query/ask
Runs RAG retrieval over indexed chunks and returns answer + translations.
